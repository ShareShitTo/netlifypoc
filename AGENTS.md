# Agent Instructions

- Use `pnpm` as the package manager for this repository.
- Do not use `npm` or `yarn` commands unless explicitly requested.
- After any code change, run `pnpm build` to confirm the project compiles before declaring work done.
- If `pnpm build` fails, fix build errors and rerun automatically unless a blocking product decision requires user input.
